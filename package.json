{
  "name": "thinkific-sunbird",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.js",
    "start": "node scripts/dev.js",
    "test": "npm --prefix backend test",
    "build": "npm --prefix ui run build",
    "install:all": "npm --prefix backend install && npm --prefix ui install"
  }
}
